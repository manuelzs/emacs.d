Usage
=====

```
# Replace .emacs.d
cd ~
rm -rf .emacs.d/
git clone git@github.com:manuelzs/emacs.d.git .emacs.d

# Update submodules
cd .emacs.d
git submodule init
git submodule update

# Start emacs
emacs
```
